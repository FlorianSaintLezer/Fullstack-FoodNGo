{% extends 'base.html.twig' %}
{% block title %}Food and Go - Edit your profile
{% endblock %}


{% block body %}

	<section class="form-login">
		{{form_start(form)}}
		{{form_widget(form)}}

		<button type="submit">
			<i class="fa fa-save" aria-hidden="true"></i>
		</button>
		{{form_end(form)}}
		<a href="{{ path('editPassword') }}">
			<i class="fa fa-lock" aria-hidden="true"></i>
		</a>
	</section>


{% endblock %}
